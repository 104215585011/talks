# LinguaAI · 评审评分表

> 一轮（或几轮）会话后，按六个维度给 agent 输出打分。
> 每个维度 0–2 分，满分 12 分。

---

## 评分维度

### 1. 正确性（Correctness）0–2
实现出来的行为是否符合 ticket 的验收标准。

| 分 | 标准 |
|----|------|
| 2 | 所有验收标准逐条通过，行为与 ticket 描述完全一致 |
| 1 | 核心路径通过，但有次要验收标准未覆盖或有轻微偏差 |
| 0 | 主功能未实现，或实现与验收标准有根本性出入 |

**LinguaAI 校准标注**：判定依据是 `feature_list.json` 中 `verification` 字段的每一条，逐条核对。不能只看 status 字段。

---

### 2. 验证（Verification）0–2
要求的检查是否真的跑过，并留下可追溯的证据。

| 分 | 标准 |
|----|------|
| 2 | tsc + lint + test + build 全部运行并通过；`feature_list.json` 的 evidence 字段有具体日期和命令输出 |
| 1 | 部分验证步骤跑了，evidence 有记录但不完整（缺少某一项） |
| 0 | 没有运行验证，或 evidence 是空字符串 / 敷衍填写（如"已测试"） |

**常见失败模式**：agent 把 status 改成 passing 但 evidence 字段留空，或写"通过"但没有日期和命令。

---

### 3. 范围纪律（Scope Discipline）0–2
是否基本保持在选定功能范围内，没有未经授权的扩散改动。

| 分 | 标准 |
|----|------|
| 2 | 改动集中在 ticket 范围内；如有范围外发现，开了新 ticket 而不是顺手改 |
| 1 | 有少量范围外改动，但改动本身无害且有记录 |
| 0 | 大量范围外改动，或改了未理解的代码，或删了没看懂的东西 |

**LinguaAI 校准标注**：CLAUDE.md 明确写"范围外的问题开 ticket"。任何改动超出当前 ticket 范围且没有在 feature_list.json 里新开条目的，扣分。

---

### 4. 可靠性（Reliability）0–2
结果是否能在重启或重跑后继续工作，没有依赖本地状态或单次运行环境。

| 分 | 标准 |
|----|------|
| 2 | 重新 `.\start-dev.ps1 3000` + `npm test` 后状态一致；没有硬编码本地路径 |
| 1 | 基本可靠，但有一个可复现的边界情况或环境依赖 |
| 0 | 依赖特定本地状态才能工作，换机器或重启后行为不稳定 |

---

### 5. 可维护性（Maintainability）0–2
代码和文档是否清楚到足以交给下一轮会话继续推进。

| 分 | 标准 |
|----|------|
| 2 | 改动符合 `docs/technical-standards.md` 命名规范；无 `any` 类型；无调试残留；改动意图从代码本身可以读懂 |
| 1 | 大体符合规范，但有轻微代码气味（小范围 any、命名不一致等） |
| 0 | 有 TypeScript 报错被忽略；有大块注释掉的代码；未遵循项目命名规范 |

---

### 6. 交接准备度（Handoff Readiness）0–2
新会话是否能只靠仓库内文件继续推进，不需要口头补充说明。

| 分 | 标准 |
|----|------|
| 2 | `session-handoff.md`、`claude-progress.md`、`feature_list.json` 全部更新；下一步动作明确 |
| 1 | 有一个文件未更新，但关键信息可以从其他文件推断 |
| 0 | 没有更新任何交接文档，新会话接手需要大量推断或人工说明 |

---

## 总分与结论

| 总分 | 结论 |
|------|------|
| 10–12 | **Accept** — 达标，可以直接继续下一个 ticket |
| 7–9 | **Revise** — 需要修补一两处才能接受，具体见备注 |
| 0–6 | **Block** — 有根本性问题，下一轮必须先修复再继续 |

---

## 评分记录

### Sprint 7（2026-05-11 ~ 2026-05-12）Codex1 实施

| 维度 | 分数 | 备注 |
|------|------|------|
| 正确性 | 2 | TICKET-701/702/703/704/710/711 验收标准全部通过，截图和测试为证 |
| 验证 | 1 | 测试从 61 → 83 条，lint/build 通过；TTS 修复前 evidence 字段有填写；但部分早期 ticket evidence 描述不够具体 |
| 范围纪律 | 2 | 改动集中在 Sprint 7 视觉和聊天区域，未发现越界改动 |
| 可靠性 | 2 | 本轮验证通过，重启后行为一致 |
| 可维护性 | 1 | TypeScript 严格模式通过；FluidBackground 组件结构清晰；但 ChatWorkspace.tsx 行数偏多，有复杂度积累风险 |
| 交接准备度 | 1 | claude-progress.md 已更新；session-handoff.md 是本轮新建，之前缺失 |
| **合计** | **9** | **Revise** → 补全 session-handoff.md 和 clean-state-checklist.md 后可升为 Accept |

---

## Evaluator 校准记录

> 每次发现 evaluator 判断与人工判断有偏差时，在此记录调整了什么标准、为什么调整。

| 日期 | 维度 | 问题 | 调整内容 |
|------|------|------|----------|
| 2026-05-12 | 验证 | agent 把 evidence 写成"通过"而非具体命令输出 | 在验证维度的 0 分标准里补充"敷衍填写"的例子 |

